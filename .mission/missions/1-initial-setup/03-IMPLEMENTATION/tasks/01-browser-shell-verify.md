---
dependsOn: ["implementation/01-browser-shell"]
agent: "copilot-cli"
---

# Verify Browser Shell

Test and validate the browser shell created in the paired implementation task. Confirm the HTML entrypoint loads the JavaScript bootstrap correctly, the canvas can mount into a full-viewport layout, and the shell supports the intended HUD overlay structure without module-loading or startup errors. Keep this task focused on tests and validation only; do not add new feature work.
