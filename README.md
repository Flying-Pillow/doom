# doom
An example repo to test the mission application

## Run locally

1. Start the static server with `npm start`
2. Open `http://127.0.0.1:8080`

Set `PORT` to use a different port, for example `PORT=3000 npm start`.
